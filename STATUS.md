# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package, offline unit tests, a local CPU smoke run, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `TASK-INFERENCE` tutorial `tutorials/swin2sr_super_resolution_colab.ipynb` (generated by `tools/build_notebook.py`, NOTEBOOK_SPEC 1.1 §3.6) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). Nothing here is clean-runtime notebook evidence; the repository is not release-grade until such evidence for the exact candidate commit is recorded in `docs/release-verification.md`.
