# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package, offline unit tests, a local CPU smoke run, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the `TASK-INFERENCE` tutorial `tutorials/swin2sr_super_resolution_colab.ipynb` (NOTEBOOK_SPEC 1.0) exist and pass static validation (`tools/validate_release_assets.py`). Nothing here is clean-runtime notebook evidence; the repository is not release-grade until such evidence for the exact candidate commit is recorded in `docs/release-verification.md`.
